{
	"workspaces": ["packages/*", "apps/*"],
	"scripts": {
		"clean": "rm -rf dist api",
		"build": "bun clean && bun --filter '*' build",
		"dev": "bun --filter '*' dev",
		"lint": "bunx biome check --write"
	},
	"dependencies": {
		"@biomejs/biome": "^1.9.2"
	}
}
