{
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "clean": "rm -rf dist api",
    "build": "bun clean && bun --filter '*' build",
    "dev": "bun --filter '*' dev"
  },
  "dependencies": {
    "@biomejs/biome": "^1.9.2"
  }
}